name: Deploy Book Carousel

on:
  push:
    branches: ["claude/book-carousel-auto-upload-xpwihz"]
    paths:
      - "book-carousel/**"
      - ".github/workflows/deploy-book-carousel.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Cloudflare Workers 배포
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          # 기존 CF_API_TOKEN이 폐기된 경우를 대비해 살아있는 토큰으로 폴백
          apiToken: ${{ secrets.CF_API_TOKEN || secrets.CF_API_TOKEN_JUMPLISH || secrets.CF_API_TOKEN_JUMPLISH_DEPLOY }}
          workingDirectory: book-carousel
          secrets: |
            ANTHROPIC_API_KEY
            TELEGRAM_BOT_TOKEN
            TELEGRAM_CHAT_ID
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
