# Veo 실측 검증 로그

> 유료 생성마다 결과를 기록. 다음 프롬프트 보정의 근거.

## v3 — 2026-07-05 (STEP 0 반영, cut1 discovery, 8s, ~$0.5)

### 개선 확인 (성공)
- [x] 해저 퇴적층 배경 (benthic 데이터 반영) + 바닥 실트 헤이즈
- [x] ROV 장비 하단 가장자리 노출 (프레임/암) → "ROV가 찍는다" 느낌 달성
- [x] 스케일 레이저 점 2개 (평행 red dots)
- [x] 후방산란(렌즈 근처 입자 블룸) — 수중 실사 단서 확보
- [x] 공기 기포(상승 기포열) 미발생
- [x] 귀 지느러미 유지 (f4·f7에서 명확)

### 남은 문제 (다음 보정 목록)
1. **상부 수직 광선 기둥**: 조명=카메라 동축 지시에도 위에서 내리꽂는 빔 생성
   (금지 문구 "no light shafts from above" 무시됨). → 보정안: "the only lights are
   attached beside the camera; nothing above the scene emits or casts light" 강화,
   또는 후처리 상단 비네트로 가림.
2. **해저가 너무 밝고 청록(얕은 바다 톤)**: 심해 해저는 좁은 광원 풀 + 어두운 주변이어야
   함. → 보정안: "only a small pool of light on the seafloor directly ahead; the
   bottom fades to black beyond the beam" + 후처리 그레이딩(② 예정)으로 보정.
3. cut1 초반 하단 검은 띠 잔존(부분) → ②의 cropdetect 자동 크롭으로 처리 예정.

### 결론
- 프롬프트만으로 80% 도달. 남은 20%(광선·밝기)는 ② 후처리(그레이딩·비네트·크롭)와
  다음 유료 테스트에서 프롬프트 보정 1회로 마무리 판단.

## v5 — 2026-07-05 (핀조명·레이저·밝기 수정, cut1 discovery, ~$0.5)

### 개선 확인 (성공)
- [x] **핀조명(상단 빔) 제거**: 'beam' 어휘 전면 삭제 효과 확인. 위에서 내리꽂는 광선 사라짐.
- [x] **공중 레이저 점 2개 제거**: 사라짐.
- [x] **심해 저노출**: 그레이딩으로 평균밝기 YAVG 23.6→11.3 (약 절반). 실제 심해 ROV 룩 근접.
- [x] ROV 장비 하단 노출·해저 유지.

### ★ 중대 회귀 (미해결)
- **공기 기포 기둥 재발생**: 프롬프트가 기포를 강하게 금지("absolutely NO air bubbles")했음에도
  Veo가 우측에 큰 상승 기포 플룸을 생성. → 물리적으로 불가능한 현상이 다시 등장.
  - 원인: "수중=기포"는 Veo의 매우 강한 prior. 텍스트 부정문만으론 확률적으로 못 막음.
  - 그레이딩이 어둡게 눌러 완화되나 근본 해결 아님.

### 판단 (다음 수)
1. **재생성(re-roll)**: Veo는 확률적 → 다른 시드는 기포 없이 나올 수 있음. cut1 1회(~$0.5)로 확인.
2. **비전 검증+자동 재생성 루프(#5)**: 프레임을 비전 모델로 판정(기포/핀조명/형태) → 위반 시
   자동 재생성. 프롬프트 한계를 구조로 보완하는 근본책. 배치 생산 전 필수.
   → bubbles가 "프롬프트만으론 불충분"을 실증했으므로, #5를 앞당길 근거가 생김.

## v6 — 2026-07-05 (9:16 사전합성 + 긍정 프롬프트, cut1 discovery, ~$0.5)

### 개선 확인 (성공)
- [x] **검은 바 완전 제거(문제1 해결)**: imageprep로 9:16 사전합성 → 풀프레임. 위아래 letterbox
      및 '바 속 기포' 소멸. Veo가 세로 확장을 상상하지 않음.
- [x] 핀조명 없음·풀프레임·덤보 형태 유지·ROV 하단 프레임 노출·심해 저노출 유지.

### 부분 개선 / 미해결
- **기포(문제2): 긍정 프롬프트로도 완전 제거 실패.** bubble/air 단어를 전부 뺐는데도
  초반 프레임에서 해저 바닥으로부터 밝은 상승 플룸 발생. 그레이딩이 상당 부분 어둡게 눌러
  후반/중반 프레임에선 옅은 실트 헤이즈 수준으로 완화되나, 초반엔 밝은 기포가 보임.

### ★ 핵심 발견 (다음 레버)
- **기포가 '해저 바닥'에서 발생.** benthic(해저 노출) 세팅이 트리거로 의심됨
  (모델이 해저 ROV = 스러스터로 인한 퇴적물/기포로 연상).
- **가설 검증안(무료·정확성 유지): pelagic(개방 수층) 프레이밍 테스트.** 덤보문어는 실제로
  해저 '위'를 호버링하므로 해저를 화면에 안 넣어도 정확함 → 해저 트리거 제거 시 기포 소멸 기대.
  트레이드오프: 사용자가 선호한 '해저 배경'을 포기.
- 그 외: 그레이딩 강화(초반 구간 추가 크러시) / 재생성(확률적) / 비전 검증 루프(#5, 유료).

## v7 — 2026-07-05 (오픈워터 프레이밍 + 조명전환 제거, cut1 discovery, ~$0.5)

### 개선 유지
- [x] 검은 바 없음(풀프레임), 핀조명 없음, 저노출 어둠, 덤보 형태 유지, ROV 하단 프레임.

### ★ 결론: 프롬프트-only로는 기포 제거 실패 (확정)
- v2·v3·v6·v7 — **4차례 escalating 프롬프트 엔지니어링**(bubble 단어 삭제 → 긍정 정적서술 →
  오픈워터 프레이밍 → 조명전환 제거)에도 **기포 플룸이 계속 발생.** 그레이딩도 강한 플룸은 못 가림.
- 원인: Veo의 '수중=역동적 기포' prior가 프롬프트 제어를 압도. 프롬프트-only는 한계 도달.
- **핵심 단서: 사용자 레퍼런스(text2video, 이미지 입력 없음)는 기포가 전혀 없었음.**
  우리 파이프라인은 img2video(NOAA 정지 이미지 입력). → **img2video가 정지 이미지에서 모션을
  만들며 기포를 '모션 필러'로 삽입한다는 가설.** 생성 모드(img2video vs text2video)가
  프롬프트 단어보다 더 큰 변수일 가능성.

### 다음 (프롬프트 추가 튜닝은 중단 — 비용 낭비)
1. text2video 모드 테스트(레퍼런스가 기포 0이었던 모드) + 우리 저노출·형태잠금. ~$0.5.
   단, text2video는 특정 NOAA 개체가 아닌 '종'을 생성 → 정확성·라이선스 전략 재검토 필요.
2. 비전 검증 + 재생성 루프(재시도 상한). 프롬프트 한계를 구조로 보완.
3. 수용(그레이딩) — v7 기준 강한 플룸은 못 가려 비권장.

## v8 — 2026-07-05 (운동 방향 통제: '상승운동 금지·하강만', cut1 discovery, ~$0.5)

### 결과: 또 실패 (초반 프레임 더 심함)
- 좌우 2개의 큰 상승 기포 플룸. 방향 통제("nothing rises upward")도 무효.
- **덤보 형태·심해 어둠·풀프레임·ROV 노출은 완벽** — 남은 유일 문제는 오직 기포.

### ★★ 최종 결론: 프롬프트-only 기포 제거 = 불가 (5전 5패, 확정)
| 시도 | 접근 | 결과 |
|---|---|---|
| v3/v5 | 명시적 부정(NO bubbles) | 기포 |
| v6 | 긍정 정적서술(명사 삭제) | 기포 |
| v7 | 오픈워터+조명전환 제거 | 기포 |
| v8 | 상승운동 방향 통제 | 기포(악화) |
→ Veo img2video의 '수중=상승기포' prior가 어떤 프롬프트 조합도 압도. **프롬프트 튜닝 종료.**

### 남은 실제 해법 (프롬프트 아님)
1. **text2video 모드** — 사용자 레퍼런스(text2video)는 기포 0이었음. 최유력 미검증 레버(~$0.5).
   트레이드오프: 특정 NOAA 개체가 아닌 '종' 생성 → 정확성·라이선스 재정리 필요.
2. **비전 검증 + 재생성 루프**(상한 N회) — 깨끗한 시드 선택. 재생성 비용.
3. **수용** — 운영자 판단('적은 양 무방'). 단 초반 강플룸 시드는 재생성 필요.

## v9 — 2026-07-05 (간결·피사체 우선 재구성, Gemini 분석 반영, cut1 discovery, ~$0.5)

### ★ 돌파구: 기포 플룸 소멸 (첫 성공)
- 상승 기포 기둥이 **사라짐.** 6전 만에 처음. → **"단순화 + 피사체 우선 + 유발어(column/
  vibration/backscatter) 제거 + 상승운동 나열 삭제"** 조합이 기포에 유효.
- 즉 기포의 실제 원인은 특정 단어 하나가 아니라 **프롬프트 과부하(길이·유발어 밀도·환경 우선)**
  였음이 사실상 확인됨.

### 새 아티팩트 (형태): 문어 위 '우산/해파리' 구조물
- 문어 위에 반투명 우산/낙하산 같은 방사형 웹 구조물이 별도로 생성됨.
- **원인 추정: anatomy_lock의 'umbrella-like web' 문구를 모델이 리터럴 우산으로 렌더** +
  img2video가 덤보의 물갈퀴를 위로 확장. (영상의 방사형 web 라인이 'umbrella-like web'과 일치)
- 형태 왜곡 = 하드룰 위반. 단 이건 **기포와 달리 특정 단어(anatomy 표현) 문제라 교정 쉬움.**

### 다음 (저비용·명확)
- anatomy_lock에서 'umbrella-like web' 제거 → 'thin webbing between the short arms'처럼
  별도 구조로 안 읽히게 재서술. cut1 재검증(~$0.5).

## v10 — 2026-07-05 ('umbrella-like web' 제거, cut1 discovery, ~$0.5)

### 성공
- [x] **우산/해파리 아티팩트 소멸** — 'umbrella-like web' 긍정문구 제거가 정확한 처방이었음.
- [x] **형태 역대 최고** — 6초 정면 덤보(두 귀지느러미·물갈퀴) 매우 우수.

### ★ 핵심 realization: 기포는 '복불복(stochastic)'
- v9 시드=기포 0, v10 시드=약한 플룸 재등장. **동일 프롬프트라도 시드마다 기포 유무가 갈림.**
- 결론: 단순화가 기포 빈도·강도를 **크게 낮췄지만 결정론적으로 제거하진 못함.**
  프롬프트-only의 실질 천장 도달. 이제 **'선택(재생성) 문제'**로 성격 전환.

### 부차 이슈 (경미)
- 상단에 ROV 램프 2개 노출(v9 단순화 때 'lamps never shown' 부정문을 뺀 결과). 실사 ROV로도
  읽히나 약간 overhead. 필요시 'lit from the camera position'로 램프 명사 회피.

### 다음 (프롬프트 튜닝 종료 — 선택 문제로 전환)
1. **수용 + 재생성**: 기포 약한/없는 시드 채택, 심한 시드만 버림(운영자 '적은 양 무방'과 정합).
2. **비전 검증 + 자동 재생성 루프**: 위 선택을 자동화(상한 N회). 배치 생산의 전제.
