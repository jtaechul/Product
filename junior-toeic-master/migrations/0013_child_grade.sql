-- 아이 학년 (2026-08-23)
--
-- 학원 시절엔 반(classes.grade·set_size)이 학년을 대신했다. 학부모가 직접 아이를
-- 만드는 지금은 반이 없어 class_id 가 NULL 이고, 그 결과 groupOf 가 기본값으로 떨어져
-- **모든 아이가 주니어(초3~4)로 판정**되고 있었다. 그래서
--   · 듣기 재생 속도가 아무리 잘해도 '기본'에서 안 올라가고
--   · 중3도 주니어용 짧은 진단(16문항)을 받았다.
-- 학년을 직접 받아 저장한다 — 가입 화면에서 부모가 한 번 고르면 끝이다.
--
-- NULL = 옛 계정(학원 발급) → 예전처럼 반의 set_size 로 판정한다.
ALTER TABLE users ADD COLUMN grade TEXT;
