# CHANGELOG — 천하쟁패 (Color Battle)

> 대화 기반으로 관리하던 프로토타입 이력 요약 이후로는 Git 커밋으로 관리

## v10 — VS 카드 · 명장 시스템 (현재)
- **장수 초상 6장 누끼 완료** → `assets/generals/*.png` (투명 배경, 머리카락 틈까지 제거). AI 매팅(rembg u2net) + 순수 흰색 연결성분 제거로 강철 갑옷 손상 없이 처리
- **VS 카드 렌더러**: 기존 도형 격투 폐기 → 좌우 초상 카드(세력색 프레임) + 이름·계급 + HP 게이지 + 충돌 섬광 + 勝/敗 도장. 초상 PNG 있으면 사용, 없으면 세력 엠블럼으로 자동 대체 (P8 카타르시스·P9 연속성)
- **명장 시스템 (P9)**: 세력별 고정 로스터 3인 + 계급(교위→비장→아장→편장군→대장군), 일기토 승리 시 전공 누적·승급. 최고 계급 명장이 출전. 세력 패널·승리 화면에 명장 노출, localStorage로 통산 전적 이어짐
- **일기토 빈도 축소**: 대군격돌 삭제 → '수도 결전'에서만 발동, 쿨다운 10→14초 (희소성=카타르시스)

## v9 — 관전 명료성 패키지
- 설계 원칙 문서 v0.9 반영 (시청 심리 검증 → 원칙 10개)
- **P3 가독성**: 국력바 세력명+지역수 라벨 + "선두" 콜아웃 / 선두 수도 왕관 / 병력 숫자 정리(소유=굵게, 중립=흐리게)
- **P2 보이는 인과**: 전투 시 "공격 vs 수비" 플로팅 숫자 + 지형/도하손실 표시 / 사기↑ 배지 + 행군 토큰 금색링 / 자원→보정효과(공격×1.5 등) 표기

## v8 — 일기토·수도 시스템 정리
- 일기토를 "수도 결전/대군 격돌"에만 + 쿨다운 / 결과를 실제 병력·자원으로 가중
- 수도 함락 시 세력 전체 흡수(absorb) + 왕 얼굴 사망 연출
- 자원 범례 제거 → 세력별 자원 패널(아이콘+수량)

## v7 — 지형 지도 재구성
- 등고선 픽셀 지형 배경 + 반투명 세력 틴트 + 굵은 경계선
- 지형 4종(바다/평야/산/강), 바다·강은 통과 가능하되 느림+손실
- 수도 표식(깃발+성+글로우)

## v6 — 곡선 경계 + 커스텀 아이콘
- 도메인 워핑으로 자연스러운 국경 곡선
- 모든 이모지 → 코드로 그린 커스텀 아이콘(문장/광물/성 등)으로 교체
- 승리 요약, 범례·자원 지도 하단 배치

## v5 — 지역(province) 단위로 전면 재설계
- 픽셀 확산 폐기 → 보로노이 ~52개 지역 + 행군 토큰 + 지역 단위 전투(지역 통째 점령 = 뭉개짐 제거)
- 자원 노드, 국력 게이지, 상세 일기토

## v1~v4 — 초기 픽셀 확산 프로토타입 (폐기)
- 색 확산 모델 반복 → "뭉개짐·원인 안 보임·단조로움"으로 방향 전환

---
### 다음(미구현)
- 공성전(수도 함락 직전 단계) 검토 — 고조-이완 리듬(P5)·카타르시스(P8) 강화 후보
- 일기토 픽셀 애니메이션(코에이 삼국지식) 연출 고도화 검토
- 명장 로스터 초상 개별화(현재는 세력 대표 초상 1장 공용) — 3인 각자 초상
- YouTube 자동 업로드 파이프라인(헤드리스 렌더 → ffmpeg)
